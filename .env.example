# =============================================================================
# Configuration Locale - FedEdge
# =============================================================================
#
# Copier ce fichier en .env et adapter selon votre environnement
#
# Usage:
#   1. cp .env.example .env
#   2. Modifier les valeurs si nécessaire
#   3. Les variables seront chargées automatiquement par python-dotenv
#
# =============================================================================

# -----------------------------------------------------------------------------
# Chemin de base des données
# -----------------------------------------------------------------------------
# Local: ./data (relatif au répertoire de travail)
# Docker: /app/data (défini dans docker-compose.yml, ne pas modifier ici)
DATA_DIR=./data

# -----------------------------------------------------------------------------
# Debug et Logging
# -----------------------------------------------------------------------------
# Afficher la configuration des chemins au démarrage
DEBUG_PATHS=true

# Désactiver le buffering Python (utile pour les logs en temps réel)
PYTHONUNBUFFERED=1

# Niveau de log (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
